import React from 'react';

// ProjectCard is now embedded directly in Projects.js for better state management.
// This file is kept for compatibility.
const ProjectCard = ({ project, index }) => null;

export default ProjectCard;
